services:
  docs:
    build:
      context: ..
      dockerfile: docs-site/Dockerfile
    ports:
      - "4321:80"
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
